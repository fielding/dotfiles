# ~/.tmux.conf
# Author: Fielding Johnston

# reload config without killing server
bind r source-file ~/.tmux.conf

# set 256 colors
set-option -g default-terminal "screen-256color-italic"

# change command prefix
set-option -g prefix C-Space

# set vi keys
setw -g mode-keys vi

# mouse support
set -w -g mode-mouse on
set -g mouse-select-window on
set -g mouse-select-pane on
set -g mouse-resize-pane on

# window splitting
unbind %
bind-key Enter split-window -h
unbind '"'
bind-key \ split-window -v

# window switching
bind-key -r C-h select-window -t :-
bind-key -r C-l select-window -t :+

# toggle statusbar
bind-key S set-option status


# Everything from here down needs to be reviewed


# bind -n C-h run "(tmux display-message -p '#{pane_current_command}' | grep -iqE '(^|\/)g?(view|n?vim?)(diff)?$' && tmux send-keys C-h) || tmux select-pane -L"
# bind -n C-j run "(tmux display-message -p '#{pane_current_command}' | grep -iqE '(^|\/)g?(view|n?vim?)(diff)?$' && tmux send-keys C-j) || tmux select-pane -D"
# bind -n C-k run "(tmux display-message -p '#{pane_current_command}' | grep -iqE '(^|\/)g?(view|n?vim?)(diff)?$' && tmux send-keys C-k) || tmux select-pane -U"
# bind -n C-l run "(tmux display-message -p '#{pane_current_command}' | grep -iqE '(^|\/)g?(view|n?vim?)(diff)?$' && tmux send-keys C-l) || tmux select-pane -R"
# bind -n C-\ run "(tmux display-message -p '#{pane_current_command}' | grep -iqE '(^|\/)g?(view|n?vim?)(diff)?$' && tmux send-keys 'C-,') || tmux select-pane -l"


#bind h select-pane -L
#bind j select-pane -D
#bind k select-pane -U
#bind l select-pane -R

# TODO: Review during os x TMUX/VIM/iterm2 setup shit
# Setup 'v' to begin selection as in Vim
bind-key -t vi-copy v begin-selection
bind-key -t vi-copy y copy-pipe "reattach-to-user-namespace pbcopy"

# Update default binding of `Enter` to also use copy-pipe
unbind -t vi-copy Enter
bind-key -t vi-copy Enter copy-pipe "reattach-to-user-namespace pbcopy"

# Bind ']' to use pbpaste
bind ] run "reattach-to-user-namespace pbpaste | tmux load-buffer - && tmux paste-buffer"

# fix scrolling
# OLD(WHAT THE FUCK IS THIS) set -g terminal-overrides 'xterm*:smcup@:rmcup@'

# scrollback buffer n lines
set -g history-limit 4096

# on-screen time for display-panes in ms
set -g display-panes-time 2000

# start window indexing at one instead of zero
set -g base-index 1

# enable wm window titles
set -g set-titles on

# border colours
set -g pane-active-border-bg default

# wm window title string (uses statusbar variables)
set -g set-titles-string "tmux:#I [ #W ]"

# OLD session initialization
# OLD bind R source-file ~/.tmux/code-ruby


#  aesthetics ---------------------------------------------------------------------------------------------------------------------------------------------

# statusbar
set -g status-utf8 on
# set -g status-interval 1
# set -g status-justify centre # center align window list
# set -g status-left-length 20
# set -g status-left '#[fg=green] #H #[fg=black]• #[fg=green,bright]#P#[default]'
# set -g status-right '#[fg=blue,bright]Up#(uptime | cut -f 4-5 -d " " | cut -f 1 -d ",") #[fg=black]• #[fg=cyan,bright]#(cut -d " " -f 1-4 /proc/loadavg) '

# default statusbar colors
set -g status-fg colour0
set -g status-bg colour236
#set -g status-attr dim

# default window title colors
# set-window-option -g window-status-fg white
# set-window-option -g window-status-bg default
# set-window-option -g window-status-attr dim

# active window title colors
# set-window-option -g window-status-current-fg white
# set-window-option -g window-status-current-bg default
# set-window-option -g window-status-current-attr bright

# command/message line colors
# set -g message-fg white
# set -g message-bg black
# set -g message-attr bright
